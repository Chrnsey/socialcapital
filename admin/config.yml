# ===========================================================================
# Decap CMS — the thing that lets you edit the website's words yourself.
#
# ONE LINE BELOW NEEDS CHANGING before this works: `repo`.
# Replace YOUR-GITHUB-USERNAME with your actual GitHub username once you
# have created the repository.
# ===========================================================================

backend:
  name: github
  repo: Chrnsey/socialcapital
  branch: main
  # Sign in through our own /oauth/ function on Cloudflare, rather than
  # Netlify's service. The function lives in functions/oauth/.
  base_url: https://socialcapital.ie
  auth_endpoint: oauth/auth

# Saves go straight to the live site. If you would rather have a "draft"
# step where changes wait for you to publish them, delete the # on the
# next line.
# publish_mode: editorial_workflow

media_folder: "images"
public_folder: "/images"

collections:
  - name: pages
    label: "Page wording"
    files:

      # ---------------------------------------------------------------
      - name: index
        label: "Homepage"
        file: "content/index.json"
        format: json
        fields:
          - name: hero
            label: "Opening section"
            widget: object
            fields:
              - { name: title, label: "Big headline", widget: string }
              - { name: body, label: "Paragraph underneath", widget: text }
              - { name: cta_primary, label: "Green button text", widget: string }
          - name: values
            label: "The three short points"
            widget: object
            fields:
              - name: items
                label: "Points"
                label_singular: "point"
                widget: list
                summary: "{{fields.title}}"
                fields:
                  - { name: title, label: "Heading", widget: string }
                  - { name: body, label: "Text", widget: text }
          - name: teasers
            label: "The three cards"
            widget: object
            fields:
              - { name: tag, label: "Small grey label", widget: string }
              - { name: title, label: "Section heading", widget: string }
              - { name: body, label: "Intro paragraph", widget: text }
              - { name: note, label: "Note under the cards", widget: string }
              - name: items
                label: "Cards"
                label_singular: "card"
                widget: list
                summary: "{{fields.name}}"
                fields:
                  - { name: marker, label: "Text in the circle (1-2 characters)", widget: string }
                  - { name: name, label: "Card heading", widget: string }
                  - { name: role, label: "Small grey line", widget: string }
                  - { name: quote, label: "Quote", widget: text }
          - name: industries
            label: "Industries section"
            widget: object
            fields:
              - { name: tag, label: "Small grey label", widget: string }
              - { name: title, label: "Heading", widget: string }
              - { name: body, label: "Paragraph (<strong> makes text bold - leave the tags in)", widget: text }
          - name: cta
            label: "Green box at the bottom"
            widget: object
            fields:
              - { name: title, label: "Heading", widget: string }
              - { name: body, label: "Paragraph", widget: text }

      # ---------------------------------------------------------------
      - name: schools
        label: "For schools page"
        file: "content/schools.json"
        format: json
        fields:
          - name: hero
            label: "Opening section"
            widget: object
            fields:
              - { name: kicker, label: "Small line above the headline", widget: string }
              - { name: title, label: "Big headline", widget: string }
              - { name: body, label: "Paragraph underneath", widget: text }
          - name: facts
            label: "The grey strip"
            widget: object
            fields:
              - { name: line, label: "Line of facts (<strong> makes text bold)", widget: text }
          - name: founding
            label: "Founding schools offer"
            widget: object
            fields:
              - { name: places, label: "How many founding places", widget: string }
              - { name: line, label: "The offer, in full", widget: text }
          - name: how
            label: "What a school gets"
            widget: object
            fields:
              - { name: title, label: "Heading", widget: string }
              - { name: body, label: "Intro paragraph", widget: text }
              - name: items
                label: "Points"
                label_singular: "point"
                widget: list
                summary: "{{fields.title}}"
                fields:
                  - { name: title, label: "Heading", widget: string }
                  - { name: body, label: "Text", widget: text }
          - name: cta
            label: "Green box at the bottom"
            widget: object
            fields:
              - { name: title, label: "Heading", widget: string }
              - { name: body, label: "Paragraph", widget: text }

      # ---------------------------------------------------------------
      - name: mentors
        label: "Become a mentor page"
        file: "content/mentors.json"
        format: json
        fields:
          - name: hero
            label: "Opening section"
            widget: object
            fields:
              - { name: kicker, label: "Small line above the headline", widget: string }
              - { name: title, label: "Big headline", widget: string }
              - { name: body, label: "Paragraph underneath", widget: text }
          - name: involves
            label: "What it involves"
            widget: object
            fields:
              - { name: tag, label: "Small grey label", widget: string }
              - { name: title, label: "Heading", widget: string }
              - { name: body, label: "Intro paragraph", widget: text }
              - name: items
                label: "Points"
                label_singular: "point"
                widget: list
                summary: "{{fields.title}}"
                fields:
                  - { name: title, label: "Heading", widget: string }
                  - { name: body, label: "Text", widget: text }
          - name: quote
            label: "Your statement"
            widget: object
            fields:
              - { name: tag, label: "Small grey label", widget: string }
              - { name: body, label: "Why you started Social Capital - your own words", widget: text }
              - { name: attribution, label: "Signed off as", widget: string }
          - name: signup
            label: "Sign-up section"
            widget: object
            fields:
              - { name: title, label: "Heading", widget: string }
              - { name: body, label: "Paragraph", widget: text }

      # ---------------------------------------------------------------
      - name: businesses
        label: "For businesses page"
        file: "content/businesses.json"
        format: json
        fields:
          - name: hero
            label: "Opening section"
            widget: object
            fields:
              - { name: kicker, label: "Small line above the headline", widget: string }
              - { name: title, label: "Big headline", widget: string }
              - { name: body, label: "Paragraph underneath", widget: text }
          - name: ways
            label: "Ways to get involved"
            widget: object
            fields:
              - { name: tag, label: "Small grey label", widget: string }
              - { name: title, label: "Heading", widget: string }
              - { name: body, label: "Intro paragraph", widget: text }
              - name: items
                label: "Ways"
                label_singular: "way"
                widget: list
                summary: "{{fields.title}}"
                fields:
                  - { name: title, label: "Heading", widget: string }
                  - { name: body, label: "Text", widget: text }
          - name: cta
            label: "Box at the bottom"
            widget: object
            fields:
              - { name: title, label: "Heading", widget: string }
              - { name: body, label: "Paragraph", widget: text }

  # -------------------------------------------------------------------
  - name: sectors
    label: "Industry descriptions"
    files:
      - name: sectors
        label: "Industry descriptions (shown in the directory)"
        file: "content/sectors.json"
        format: json
        fields:
          - name: sectors
            label: "Industries"
            label_singular: "industry"
            widget: list
            summary: "{{fields.title}}"
            fields:
              - name: slug
                label: "Internal code — MUST match the industry saved on mentors, so pick from the list"
                widget: select
                options:
                  - technology
                  - healthcare
                  - law
                  - finance
                  - trades
                  - media
                  - education
                  - engineering
                  - public_sector
                  - other
              - { name: title, label: "Name shown on screen", widget: string }
              - { name: summary, label: "Description of the sector", widget: text }

  # -------------------------------------------------------------------
  - name: routes
    label: "Ways in"
    files:
      - name: routes
        label: "Ways in"
        file: "content/routes.json"
        format: json
        fields:
          - name: section
            label: "Top of this section's page"
            widget: object
            fields:
              - { name: slug, label: "Internal code - do not change", widget: string }
              - { name: tag, label: "Small grey label", widget: string }
              - { name: title, label: "Section heading", widget: string }
              - { name: intro, label: "Section intro paragraph", widget: text }
          - name: items
            label: "Ways in"
            label_singular: "way in"
            widget: list
            summary: "{{fields.title}}"
            fields:
              - { name: slug, label: "Web address (lowercase, hyphens - changing this breaks links)", widget: string }
              - { name: title, label: "Title", widget: string }
              - { name: subtitle, label: "Small line above the title", widget: string, required: false }
              - { name: summary, label: "One-line summary (shown on the card)", widget: string }
              - { name: checked, label: "Information last checked (e.g. September 2026)", widget: string }
              - { name: body, label: "Main explanation (blank line = new paragraph)", widget: text }
              - name: facts
                label: "Quick facts box (optional)"
                label_singular: "fact"
                widget: list
                required: false
                summary: "{{fields.label}}"
                fields:
                  - { name: label, label: "Label", widget: string }
                  - { name: value, label: "Value", widget: string }
              - name: blocks
                label: "Sections"
                label_singular: "section"
                widget: list
                required: false
                summary: "{{fields.heading}}"
                fields:
                  - { name: heading, label: "Heading", widget: string }
                  - { name: text, label: "Text", widget: text }
              - name: myths
                label: "What people get wrong (optional)"
                label_singular: "myth"
                widget: list
                required: false
                summary: "{{fields.myth}}"
                fields:
                  - { name: myth, label: "The myth", widget: string }
                  - { name: reality, label: "The reality", widget: text }
              - name: apply
                label: "Official links"
                label_singular: "link"
                widget: list
                required: false
                summary: "{{fields.label}}"
                fields:
                  - { name: label, label: "Link text", widget: string }
                  - { name: url, label: "Address", widget: string }
              - name: related
                label: "Read next (links to other pages on this site)"
                label_singular: "link"
                widget: list
                required: false
                summary: "{{fields.label}}"
                fields:
                  - { name: label, label: "Link text", widget: string }
                  - { name: href, label: "Address, e.g. /funding/hear", widget: string }

  # -------------------------------------------------------------------
  - name: funding
    label: "Paying for it"
    files:
      - name: funding
        label: "Paying for it"
        file: "content/funding.json"
        format: json
        fields:
          - name: section
            label: "Top of this section's page"
            widget: object
            fields:
              - { name: slug, label: "Internal code - do not change", widget: string }
              - { name: tag, label: "Small grey label", widget: string }
              - { name: title, label: "Section heading", widget: string }
              - { name: intro, label: "Section intro paragraph", widget: text }
          - name: items
            label: "Funding options"
            label_singular: "funding option"
            widget: list
            summary: "{{fields.title}}"
            fields:
              - { name: slug, label: "Web address (lowercase, hyphens - changing this breaks links)", widget: string }
              - { name: title, label: "Title", widget: string }
              - { name: subtitle, label: "Small line above the title", widget: string, required: false }
              - { name: summary, label: "One-line summary (shown on the card)", widget: string }
              - { name: checked, label: "Information last checked (e.g. September 2026)", widget: string }
              - { name: body, label: "Main explanation (blank line = new paragraph)", widget: text }
              - name: facts
                label: "Quick facts box (optional)"
                label_singular: "fact"
                widget: list
                required: false
                summary: "{{fields.label}}"
                fields:
                  - { name: label, label: "Label", widget: string }
                  - { name: value, label: "Value", widget: string }
              - name: blocks
                label: "Sections"
                label_singular: "section"
                widget: list
                required: false
                summary: "{{fields.heading}}"
                fields:
                  - { name: heading, label: "Heading", widget: string }
                  - { name: text, label: "Text", widget: text }
              - name: myths
                label: "What people get wrong (optional)"
                label_singular: "myth"
                widget: list
                required: false
                summary: "{{fields.myth}}"
                fields:
                  - { name: myth, label: "The myth", widget: string }
                  - { name: reality, label: "The reality", widget: text }
              - name: apply
                label: "Official links"
                label_singular: "link"
                widget: list
                required: false
                summary: "{{fields.label}}"
                fields:
                  - { name: label, label: "Link text", widget: string }
                  - { name: url, label: "Address", widget: string }
              - name: related
                label: "Read next (links to other pages on this site)"
                label_singular: "link"
                widget: list
                required: false
                summary: "{{fields.label}}"
                fields:
                  - { name: label, label: "Link text", widget: string }
                  - { name: href, label: "Address, e.g. /funding/hear", widget: string }

  # -------------------------------------------------------------------
  - name: careers
    label: "Careers"
    files:
      - name: careers
        label: "Careers"
        file: "content/careers.json"
        format: json
        fields:
          - name: section
            label: "Top of this section's page"
            widget: object
            fields:
              - { name: slug, label: "Internal code - do not change", widget: string }
              - { name: tag, label: "Small grey label", widget: string }
              - { name: title, label: "Section heading", widget: string }
              - { name: intro, label: "Section intro paragraph", widget: text }
          - name: items
            label: "Careers"
            label_singular: "career"
            widget: list
            summary: "{{fields.title}}"
            fields:
              - { name: slug, label: "Web address (lowercase, hyphens - changing this breaks links)", widget: string }
              - { name: title, label: "Title", widget: string }
              - { name: subtitle, label: "Small line above the title", widget: string, required: false }
              - { name: summary, label: "One-line summary (shown on the card)", widget: string }
              - { name: checked, label: "Information last checked (e.g. September 2026)", widget: string }
              - { name: body, label: "Main explanation (blank line = new paragraph)", widget: text }
              - name: facts
                label: "Quick facts box (optional)"
                label_singular: "fact"
                widget: list
                required: false
                summary: "{{fields.label}}"
                fields:
                  - { name: label, label: "Label", widget: string }
                  - { name: value, label: "Value", widget: string }
              - name: blocks
                label: "Sections"
                label_singular: "section"
                widget: list
                required: false
                summary: "{{fields.heading}}"
                fields:
                  - { name: heading, label: "Heading", widget: string }
                  - { name: text, label: "Text", widget: text }
              - name: myths
                label: "What people get wrong (optional)"
                label_singular: "myth"
                widget: list
                required: false
                summary: "{{fields.myth}}"
                fields:
                  - { name: myth, label: "The myth", widget: string }
                  - { name: reality, label: "The reality", widget: text }
              - name: apply
                label: "Official links"
                label_singular: "link"
                widget: list
                required: false
                summary: "{{fields.label}}"
                fields:
                  - { name: label, label: "Link text", widget: string }
                  - { name: url, label: "Address", widget: string }
              - name: related
                label: "Read next (links to other pages on this site)"
                label_singular: "link"
                widget: list
                required: false
                summary: "{{fields.label}}"
                fields:
                  - { name: label, label: "Link text", widget: string }
                  - { name: href, label: "Address, e.g. /funding/hear", widget: string }
